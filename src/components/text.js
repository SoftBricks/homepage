import React from "react";
import styled from "styled-components";

const Text = {
  Detail: styled.span`
    line-height: 1.3;
    font-size: 0.9rem;
    white-space: pre-line;
  `
};
export default Text;
