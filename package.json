{
  "name": "softbricks-homepage",
  "description": "Softbricks Homepage",
  "version": "1.0.0",
  "author": "Lukas Sporrer <lukas.sporrer@softbricks.de>",
  "dependencies": {
    "gatsby": "^1.9.98",
    "gatsby-link": "^1.6.8",
    "gatsby-plugin-google-fonts": "0.0.3",
    "gatsby-plugin-react-helmet": "^1.0.3",
    "gatsby-plugin-styled-components": "1.0.5"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "dev": "gatsby develop",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "devDependencies": {
    "prettier": "^1.5.2"
  }
}
